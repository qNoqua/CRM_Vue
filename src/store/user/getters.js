export const getters = {
    isAuthorized (state) {
        return state.isAuthorized
    },
    user (state) {
        return state.user
    },
    history (state) {
        return state.history
    }
}